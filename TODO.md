# PyLocalSend

安全的局域网文件传输工具，支持 CLI 和 WebUI 界面。

## 功能特性

- **CLI 和 WebUI 界面**
- **端到端加密** - 确保传输安全
- **PIN 验证** - 确认接收设备身份
- **分片传输** - 支持大文件传输
- **断点续传** - 传输中断后可继续
- **并行传输** - 多文件同时传输
- **文件夹同步** - 双向同步本地文件夹

## 使用方法

### CLI 方法

#### 发送端命令

- `pylocalsend gui`
  - 启动发送端 WebUI 界面
- `pylocalsend sender`
  - 启动发送端，等待接收端连接，默认打开接收端 WebUI 界面
- `pylocalsend upload <file1> <dir1> <file2> <dir2> ...`
  - 发送端提供文件或文件夹给接收端下载
- `pylocalsend rm <file1> <dir1> <file2> <dir2> ...`
  - 发送端移除已经上传的文件或文件夹
- `pylocalsend ls`
  - 列出服务器文件
- `pylocalsend config-show [--<config-item>]`
  - 查看配置，可指定具体配置项
- `pylocalsend config-set --<config-item> <value>`
  - 设置配置项，指定具体配置项和值
- `pylocalsend gen-pin`
  - 生成 PIN，用于接收端验证发送端身份
- `pylocalsend receiver new`
  - 创建一个新的接收端，指定接收端名称和 PIN，生成一个接收端链接，接收端可以直接访问 webui 界面连接
- `pylocalsend receiver rm`
  - 删除一个接收端
- `pylocalsend receiver ls`
  - 列出所有接收端，包括接收端名称，接收端链接，接收端状态
- `pylocalsend status`
  - 查看发送端状态，包括连接状态，上传状态，下载状态等
- `pylocalsend close`
  - 关闭发送端服务，断开所有连接

#### 接收端命令

- `pylocalsend ls-remote --host|-h http://192.168.1.100:8080 --pin xxxxxx`
  - 列出远程服务器可下载的文件
- `pylocalsend connect --host|-h http://192.168.1.100:8080 --pin xxxxxx`
  - 连接远程服务器，后续命令无需重复指定主机和 PIN
- `pylocalsend download <file1> <file2> <dir1> <dir2> ...`
  - 下载远程文件或文件夹
  - 并发下载多个文件
  - 支持断点续传
  - 要有好看的多文件并行化下载进度条

### GUI 方法

UI 使用 nicegui 框架，提供一个简洁的 GUI 界面，原研哉风格，简洁，美观，大方，实用。

#### 发送端 GUI 界面

`pylocalsend gui` 启动后，自动打开一个 GUI 界面，用户可以在该界面查看和管理发送端的文件传输。

整体上采用多个 tab，每个 tab 对应一个功能模块。

- **文件传输**
  - 界面上有上传的文件和文件夹的绝对路径输入框，和已上传的文件文件夹列表，点击文件夹可以弹窗展示文件夹的具体内容列表
  - 不支持拖拽和启动文件选择器，因为浏览器端无法获取绝对路径
  - 查看已上传的文件列表，包括文件大小、上传时间、上传状态
  - 查看每个文件和文件夹的下载记录，包括下载者，下载时间
  - 删除已上传的文件或文件夹
- **接收端管理**
  - 创建接收端，指定接收端名称和 PIN，生成一个接收端链接，接收端可以直接通过链接访问 webui 界面连接，不需要再输入密码
  - 删除接收端，阻止该接收端下载文件
  - 查看已创建的接收端列表，包括接收端名称、接收端链接、接收端状态
  - 查看每个接收端的下载记录，包括下载者，下载的文件列表、下载时间、下载状态
- **设置**
  - 查看和修改发送端的配置项，包括分片大小、最大并行传输数、加密状态、PIN验证状态等

#### 接收端 Web UI 界面

使用 nicegui 框架，提供一个简洁的 WebUI 界面，原研哉风格，简洁，美观，大方，实用。

- **文件传输**
  - 查看可以下载的文件列表，包括文件大小、上传时间、上传状态
  - 选择要下载的文件或文件夹
  - 指定下载目录
  - 点击下载按钮，开始下载
    - 支持并发下载多个文件
    - 支持断点续传
    - 要有好看的多文件并行化下载进度条

## 主要逻辑思考

### 文件上传和下载零拷贝

发送端永远不会复制文件内容到 GUI 端，只会将文件绝对路径、大小、修改时间等信息传递给 GUI 端，GUI 端根据这些信息显示文件列表。发送端和接收端都不会复制文件内容，而是通过流式传输，保证不会占用大量内存。

1. 发送端用户输入文件或文件夹的绝对路径，点击上传按钮，确认上传
2. GUI 端获取文件或文件夹的绝对路径、大小、修改时间等信息，并显示文件列表
3. 接收端获取到文件或文件夹的名称，并显示文件列表
4. 接收端选择下载时，浏览器还是 CLI 都通过流式下载文件内容到接收端本地，发送端和接收端都不会复制文件内容，而是通过流式传输，保证不会占用大量内存。

## 代码架构

- pylocalsend
  - cli/
  - gui/
    - sender/
    - receiver/
  - core/
    - transfer/
    - receiver/
    - file_handler/
    ...
  - utils/
    - db/
    - crypto/
    ...
- tests/
  - data/                 # .gitignore
  - gen.py                # 生成测试数据
  ...
- .gitignore
- pyproject.toml
- README.md

要求 core/ 提供主要业务逻辑代码，导出给 gui/ 和 cli/ 调用。